export const metadata = {
  title: "Services | Onwa na Aku",
  description: "Detailed breakdown of our premium maritime and logistics services.",
};

export default function ServicesLayout({ children }) {
  return <>{children}</>;
}
